特征选择方法:
    1.正则化 L1 L2正则化方法

    2.wrappers
        1.add 使用一个特征,逐步添加特征到模型中,在validation中计算err,确定特征子集
	2.remove 先使用所有特征,然后从中一点点拿出去一些特征,在validation上计算err,确定特征子集

    3.greedy algorithms 贪心算法 一个一个的组合特征
        1.每个决策都可以看成一个特征
	2.对决策树进行剪枝

	ensembles:
	    1.涉及集成分类器几个特征
	    2.随机森林
	    3.boosting

mRMR 最大相关 最小冗余
     特征与类别要相关性最大,特征间的相关性要小
